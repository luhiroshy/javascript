let usercerto = "admin";
let senhacerta = "1234";
let vezes = 1;

while (vezes <= 3) {
    let user = prompt("Digite o usuário: ");
    let senha = prompt("Digite a senha: ");
    
    if (user === usercerto && senha === senhacerta) {
        console.log("Usuário e senha correta");
}
    }
